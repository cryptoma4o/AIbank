replicaCount: 1

image:
  repository: aibank/abs-connector
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8093

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70

env:
  REDIS_URL: "redis://redis:6379"
  AUDIT_URL: "http://audit-service:8081"
  ADAPTER_URLS: ""
