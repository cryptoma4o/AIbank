replicaCount: 2

image:
  repository: aibank/onboarding-orchestrator
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8084

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

temporal:
  address: ""  # uses global if empty
