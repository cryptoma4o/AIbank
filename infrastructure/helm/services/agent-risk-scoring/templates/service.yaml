apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-risk-scoring.fullname" . }}
  labels:
    {{- include "agent-risk-scoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-risk-scoring.selectorLabels" . | nindent 4 }}
