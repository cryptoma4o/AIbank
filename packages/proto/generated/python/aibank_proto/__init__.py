"""aibank_proto — generated gRPC stubs for the AIbank platform.

Sub-packages (one per canonical context) are produced by ``buf generate`` and
live alongside this ``__init__.py``. Until codegen has run in CI this package
is empty but importable so that downstream services can declare the dependency
without conditional handling.
"""

__version__ = "0.1.0"
