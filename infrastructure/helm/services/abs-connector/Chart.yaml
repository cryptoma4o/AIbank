apiVersion: v2
name: abs-connector
description: AIbank abs-connector — ABS facade — routes canonical commands to tenant-specific adapters via Redis dedup
type: application
version: 0.1.0
appVersion: "0.1.0"
