apiVersion: v2
name: identity-service
description: AIbank Identity Service — Client authentication (ЕСИА, УКЭП, OTP) and employee SSO via Keycloak
type: application
version: 0.1.0
appVersion: "0.1.0"
