apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-reconciliation.fullname" . }}
  labels:
    {{- include "agent-reconciliation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-reconciliation.selectorLabels" . | nindent 4 }}
