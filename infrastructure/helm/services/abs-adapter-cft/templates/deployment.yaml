apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "abs-adapter-cft.fullname" . }}
  labels:
    {{- include "abs-adapter-cft.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "abs-adapter-cft.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "abs-adapter-cft.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: abs-adapter-cft
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 9001
              protocol: TCP
          env:
            - name: AUDIT_URL
              value: {{ .Values.env.AUDIT_URL | quote }}
            - name: CFT_ENDPOINT
              value: {{ .Values.env.CFT_ENDPOINT | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9001
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9001
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
