apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "abs-adapter-rs-bank.fullname" . }}
  labels:
    {{- include "abs-adapter-rs-bank.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "abs-adapter-rs-bank.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "abs-adapter-rs-bank.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: abs-adapter-rs-bank
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 9003
              protocol: TCP
          env:
            - name: AUDIT_URL
              value: {{ .Values.env.AUDIT_URL | quote }}
            - name: RSBANK_ENDPOINT
              value: {{ .Values.env.RSBANK_ENDPOINT | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9003
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9003
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
