apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ext-fssp.fullname" . }}
  labels:
    {{- include "ext-fssp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "ext-fssp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ext-fssp.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: ext-fssp
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8092
              protocol: TCP
          env:
            - name: FSSP_API_URL
              value: {{ .Values.env.FSSP_API_URL | quote }}
            - name: AUDIT_URL
              value: {{ .Values.env.AUDIT_URL | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8092
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8092
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
