apiVersion: v1
kind: Service
metadata:
  name: {{ include "abs-connector.fullname" . }}
  labels:
    {{- include "abs-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8093
      protocol: TCP
      name: http
  selector:
    {{- include "abs-connector.selectorLabels" . | nindent 4 }}
