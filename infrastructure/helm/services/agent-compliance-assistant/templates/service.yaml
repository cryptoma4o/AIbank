apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-compliance-assistant.fullname" . }}
  labels:
    {{- include "agent-compliance-assistant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-compliance-assistant.selectorLabels" . | nindent 4 }}
