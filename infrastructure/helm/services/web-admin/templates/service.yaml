apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-admin.fullname" . }}
  labels:
    {{- include "web-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-admin.selectorLabels" . | nindent 4 }}
