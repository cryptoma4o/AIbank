apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-conversational.fullname" . }}
  labels:
    {{- include "agent-conversational.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-conversational.selectorLabels" . | nindent 4 }}
