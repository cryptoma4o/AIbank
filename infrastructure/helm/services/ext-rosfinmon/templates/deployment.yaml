apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ext-rosfinmon.fullname" . }}
  labels:
    {{- include "ext-rosfinmon.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "ext-rosfinmon.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ext-rosfinmon.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: ext-rosfinmon
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8091
              protocol: TCP
          env:
            - name: ROSFINMON_API_URL
              value: {{ .Values.env.ROSFINMON_API_URL | quote }}
            - name: AUDIT_URL
              value: {{ .Values.env.AUDIT_URL | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8091
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
