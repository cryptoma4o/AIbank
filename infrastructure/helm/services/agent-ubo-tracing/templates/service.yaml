apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-ubo-tracing.fullname" . }}
  labels:
    {{- include "agent-ubo-tracing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-ubo-tracing.selectorLabels" . | nindent 4 }}
