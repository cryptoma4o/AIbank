apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubo-service.fullname" . }}
  labels:
    {{- include "ubo-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8088
      protocol: TCP
      name: http
  selector:
    {{- include "ubo-service.selectorLabels" . | nindent 4 }}
